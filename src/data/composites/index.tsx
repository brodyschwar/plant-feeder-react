import { BehaviorTreeNode } from ".."

export const SEQUENCE_DEFAULT: BehaviorTreeNode = {
    label: "Sequence"
}

export const SELECTOR_DEFAULT: BehaviorTreeNode = {
    label: "Selector"
}

export const PARRALLEL_DEFAULT: BehaviorTreeNode = {
    label: "Parallel"
}